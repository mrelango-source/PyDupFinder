# PyDupeFinder

Python script to find and delete duplicate files in any folder using MD5 hashing.

### Features
- Scans folders recursively 
- Detects duplicate files even with different names
- Saves 100% storage by removing copies
- Tested on 10GB+ data, identified 0.33 MB duplicates

### How to Run
```bash
python PyDupeFinder.py
